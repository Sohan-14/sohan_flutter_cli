import 'package:sohan_flutter_cli/sohan_flutter_cli.dart';
import 'package:test/test.dart';

/// Tests the `setupProject` method for a successful setup scenario.
///
/// This test ensures that the project is set up correctly when all
/// dependencies and templates are available.
void main() {
  test('calculate', () {
    expect(calculate(), 42);
  });
}
